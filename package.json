{
  "name": "dist_node",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "client-build":"cd dist_react && yarn build",
    "client-install": "cd dist_react && yarn install",
    "start": "node index.js",
    "client": "yarn start --prefix client",
    "heroku-postbuild":"yarn run client-install && yarn run client-build"
  
  },
  "engines": {
    "yarn": "1.22.5"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  }
}
